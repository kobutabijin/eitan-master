# えいたんマスター 動作確認の手順（Windows）

index.html をダブルクリックしただけでは真っ白になります。これは正常です。
このアプリは React という仕組みで作られているため、「開発用サーバー」を起動してブラウザからアクセスする必要があります。

以下の手順どおりに進めてください。

## 1. Node.js をインストールする

1. ブラウザで https://nodejs.org/ja/ を開く
2. 「LTS」と書かれた緑色のボタン（推奨版）をクリックしてダウンロード
3. ダウンロードした `.msi` ファイルをダブルクリックし、インストーラーの指示に従って「Next」を押し続けてインストール
4. インストール後、念のためパソコンを再起動

インストールできたか確認する方法：

1. スタートメニューで「PowerShell」または「コマンドプロンプト」と検索して開く
2. 次のコマンドを入力して Enter

   ```
   node -v
   ```

   `v22.xx.x` のようにバージョンが表示されればOKです。

## 2. zipファイルを展開する

1. お送りした `eitan-master.zip` をデスクトップなど分かりやすい場所に保存
2. zipファイルを右クリック →「すべて展開」を選び、展開する

展開すると `eitan-master` フォルダができます。

## 3. フォルダをターミナルで開く

1. 展開してできた `eitan-master` フォルダを開く
2. フォルダの中の何もない場所で **Shiftキーを押しながら右クリック**
3. メニューから「PowerShell ウィンドウをここで開く」（または「ターミナルで開く」）を選択

   ※ 見つからない場合は、PowerShellを普通に開いて `cd` コマンドでフォルダまで移動してもOKです。
   例：`cd Desktop\eitan-master`

## 4. 必要な部品をインストールする

開いたPowerShell（黒いor青い画面）に、次のコマンドを入力してEnter：

```
npm install
```

少し時間がかかりますが、これで必要なファイルが自動的にダウンロードされます。
（`node_modules` というフォルダができれば成功です）

## 5. アプリを起動する

続けて次のコマンドを入力してEnter：

```
npm run dev
```

このような表示が出れば起動成功です：

```
VITE vX.X.X  ready in XXX ms

➜  Local:   http://localhost:5173/
```

## 6. ブラウザで開く

表示された `http://localhost:5173/`（数字はパソコンによって違うことがあります）を
ブラウザ（Chrome や Edge）のアドレス欄にコピーして開いてください。

「えいたんマスター」のホーム画面が表示されれば成功です！

## 終了するとき

PowerShellの画面で `Ctrl + C` を押すとサーバーが止まります。
また使いたいときは、手順5（`npm run dev`）だけやり直せばOKです（手順4は最初の1回だけで大丈夫です）。

---

## うまくいかないときは

- `npm install` や `npm run dev` でエラーが出た場合は、そのエラーメッセージをコピーして教えてください
- Node.js のインストール後もうまくいかない場合は、パソコンを再起動してから再度お試しください
