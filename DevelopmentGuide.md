# 中学受験生向け英単語学習アプリ 企画書

## 1. 概要

| 項目 | 内容 |
|---|---|
| アプリ名（仮） | えいたんマスター |
| 対象ユーザー | 小学生（中学受験生） |
| 目的 | 中学受験でよく出る英単語100個を、楽しく効率的に暗記する |
| 学習単語数 | 100語 |
| プラットフォーム | Webアプリ（ブラウザで動作） |
| 技術スタック | React + Vite |

## 2. コンセプト

- 塾や自宅学習の"すきま時間"に使えるシンプルな単語学習ツール
- 難しい操作は不要。ボタンを押すだけで進められるUI
- 小学生が飽きずに続けられるよう、○×や正解数などのフィードバックを見せる
- 保護者が管理しなくても、子どもが一人で使えることを重視

## 3. 対象ユーザー像（ペルソナ）

- 小学4〜6年生、中学受験を控えている
- スマホやタブレット、PCのブラウザで学習
- 1回の学習時間は5〜15分程度を想定
- ローマ字は読めるが、英語自体は初学者レベル

## 4. 主要機能

### 4.1 単語一覧表示機能
- 100単語を一覧で表示（英単語／日本語訳／必要に応じて発音記号や例文）
- カテゴリやレベルでの絞り込み（余裕があれば）
- 覚えた単語にチェックを付けられる

### 4.2 暗記カード機能（フラッシュカード）
- 1枚ずつカードを表示し、タップ（クリック）で英単語⇔日本語訳を切り替え
- 「わかった」「まだ覚えていない」などのボタンで自己評価
- 次のカードへ進む／前のカードに戻る操作
- 未習得の単語だけを再表示する復習モード（余裕があれば）

### 4.3 クイズ機能
- 4択問題形式（英単語→日本語訳、または日本語訳→英単語）
- 出題順のランダム化
- 正解／不正解をその場でフィードバック
- 最後に結果画面（正解数、正答率、間違えた単語一覧）

### 4.4 共通・付加機能（任意・優先度低）
- 学習の進捗状況の記録（ローカル保存）
- 覚えた単語数などの簡単な統計表示
- 音声読み上げ（Web Speech API）

## 5. 画面構成（ワイヤーフレーム概要）

```
[ホーム画面]
  ├─ 「単語一覧」ボタン
  ├─ 「暗記カード」ボタン
  └─ 「クイズ」ボタン

[単語一覧画面]
  └─ 100語のリスト（英単語・日本語訳・チェック状態）

[暗記カード画面]
  └─ カード表示 → タップで反転 → 「わかった/まだ」→ 次のカード

[クイズ画面]
  └─ 問題文 → 4択選択肢 → 正誤表示 → 次の問題
  └─ 全問終了後 → 結果画面
```

## 6. データ設計（単語データの持ち方）

100単語分のデータをJSON形式で保持する想定。

```json
[
  {
    "id": 1,
    "word": "apple",
    "meaning": "りんご",
    "level": "basic",
    "learned": false
  }
]
```

- `id`：単語の識別番号
- `word`：英単語
- `meaning`：日本語訳
- `level`：難易度やカテゴリ（任意）
- `learned`：暗記済みフラグ（ローカル保存用）

## 7. 非機能要件

- スマホ・タブレット・PCいずれでも見やすいレスポンシブデザイン
- 通信不要でも動作するくらい軽量（データはアプリ内に同梱）
- 学習データはブラウザのlocalStorageに保存し、次回起動時も引き継げるようにする
- 小学生でも読みやすい大きめの文字・シンプルな配色

## 8. 技術構成

| 項目 | 内容 |
|---|---|
| フレームワーク | React（Vite） |
| 言語 | JavaScript（またはTypeScript） |
| スタイリング | CSS（またはTailwind CSSなど、シンプルさ優先なら素のCSSでも可） |
| 状態管理 | React標準のuseState / useContext（規模的にReduxなどは不要） |
| データ保存 | localStorage（進捗・暗記済みフラグ） |
| ルーティング | react-router-dom（画面数が少ないため簡易な条件分岐でも可） |
| ホスティング | Vercel / Netlify / GitHub Pages などの静的ホスティング |

## 9. マイルストーン（開発フェーズ）

1. 環境構築・単語データ準備
2. 単語一覧機能の実装
3. 暗記カード機能の実装
4. クイズ機能の実装
5. スタイリング・UI調整
6. 動作確認・デプロイ

---

# 開発手順書

## STEP 1：環境構築

```bash
# Viteプロジェクトの作成
npm create vite@latest eitan-master -- --template react

cd eitan-master
npm install

# ルーティングを使う場合
npm install react-router-dom

# 開発サーバー起動
npm run dev
```

**ディレクトリ構成の例**

```
eitan-master/
├─ public/
├─ src/
│  ├─ data/
│  │  └─ words.json          # 100単語のデータ
│  ├─ components/
│  │  ├─ WordList.jsx        # 単語一覧画面
│  │  ├─ FlashCard.jsx       # 暗記カード画面
│  │  ├─ Quiz.jsx            # クイズ画面
│  │  └─ Home.jsx            # ホーム画面
│  ├─ hooks/
│  │  └─ useProgress.js      # localStorage連携の進捗管理フック
│  ├─ App.jsx
│  └─ main.jsx
├─ index.html
└─ package.json
```

## STEP 2：単語データの準備

- 中学受験でよく出る英単語100個を洗い出し、`src/data/words.json` にまとめる
- 各単語に `id`, `word`, `meaning` を最低限持たせる
- 必要に応じて例文や発音記号、カテゴリ分けを追加

## STEP 3：ホーム画面の実装

- 「単語一覧」「暗記カード」「クイズ」への導線となるボタンを配置
- react-router-dom を使う場合は各機能を別ルートとして実装
  - `/` → ホーム
  - `/list` → 単語一覧
  - `/flashcard` → 暗記カード
  - `/quiz` → クイズ

## STEP 4：単語一覧機能の実装

- `words.json` を読み込み、テーブルまたはカード形式でリスト表示
- 各行に「覚えた」チェックボックスを設置し、状態をlocalStorageに保存
- 検索・絞り込み機能は余裕があれば追加

## STEP 5：暗記カード機能の実装

- 現在の単語インデックスを `useState` で管理
- カードをクリック/タップすると英単語⇔日本語訳が切り替わる（CSSのtransformで反転アニメーションを付けると良い）
- 「わかった」「まだ」ボタンで次のカードへ進み、正誤情報をlocalStorageに記録
- 全カード終了後は結果や振り返り画面を表示

## STEP 6：クイズ機能の実装

- 出題する単語をランダムに選択（`words.json` からシャッフル）
- 正解の意味＋ダミーの意味3つをランダムに組み合わせて4択を生成
- 選択肢をクリックすると正誤を判定し、色や記号でフィードバック
- 全問終了後に正答率・間違えた単語一覧を表示する結果画面へ

## STEP 7：進捗管理（共通ロジック）

- `useProgress` などのカスタムフックを作成し、以下を一元管理
  - 単語ごとの「覚えたかどうか」フラグ
  - クイズの正答率・履歴
  - localStorageへの読み書き

```js
// 例: useProgress.js の骨子
import { useState, useEffect } from "react";

export function useProgress() {
  const [progress, setProgress] = useState(() => {
    const saved = localStorage.getItem("progress");
    return saved ? JSON.parse(saved) : {};
  });

  useEffect(() => {
    localStorage.setItem("progress", JSON.stringify(progress));
  }, [progress]);

  const markLearned = (id, learned) => {
    setProgress((prev) => ({ ...prev, [id]: learned }));
  };

  return { progress, markLearned };
}
```

## STEP 8：スタイリング・UI調整

- 小学生向けに、文字サイズを大きめ・配色をやさしい色合いに調整
- ボタンは押しやすい大きさに（タップ操作を意識）
- レスポンシブ対応（スマホ・タブレットでの表示確認）

## STEP 9：動作確認・テスト

- 100単語すべてが正しく表示・出題されるか確認
- localStorageの保存・復元が正しく動くか確認（ブラウザ再読み込みでチェック）
- スマホ・タブレット・PCの各画面サイズで表示崩れがないか確認

## STEP 10：デプロイ

```bash
npm run build
```

- `dist` フォルダが生成されるので、Vercel / Netlify / GitHub Pages などにデプロイ
- 例：Vercelの場合はGitHubリポジトリと連携するだけで自動デプロイ可能

---

## 今後の拡張アイデア（任意）

- 単語の音声読み上げ（Web Speech API）
- 学年・分野別の単語セット切り替え
- 学習カレンダー・連続学習日数の表示（継続のモチベーション向上）
- 保護者向けの学習状況確認ページ